interface G
{
	private int i = 0;
	protected void test1();
}




/*
		all members of an interface are public by default.
		if we want to provode an access level explicitely, then specify only public.
		we should not use any other access levels.
*/